{
  "version": 2,
  "builds": [
    { "src": "frontend/**", "use": "@vercel/static-build" },
    { "src": "backend/**", "use": "@vercel/python" }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/main.py"
    },
    { "src": "/(.*)", "dest": "frontend/$1" }
  ]
}
